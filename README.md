# jaeminlang

재민랭은 한국어와 그 외 기호로 코딩할 수 있는 프로그래밍 언어입니다.  

|키워드|기능|
|-----|-----|
|안산|출력|
|재민|입력|
|그램|변수 선언, 대입|
|러스트|반복|
|여친|다른 언어의 NULL과 똑같습니다.|
|$|구분자|

## 실행

재민랭 소스코드를 실행하시려면 릴리즈로 가셔서  
자신의 OS에 맞게 실행 파일을 다운로드 하신 후  

리눅스의 경우:
```sh
chmod +x jaeminlang # 권한 주기 (한번만 실행)

./jaeminlang source.jml
```

윈도우의 경우:  
```cmd
jaeminlang.exe source.jml
```

## 코드 작성법

### 문법

1. 구분자
키워드에 인수는 `$`로 구분합니다.  
예시: `안산$3`  

2. 주석
주석따위는 없습니다. 행운을 빕니다.  

3. 문자열
문자열을 입력할때는 `"`로 열고 닫습니다.  
예시: `안산$"\$300"`

### 변수

변수는 `그램`으로 선언하고 대입할 수 있습니다.  

|파라메터|비고|
|------|------|
|변수 이름|띄어쓰기를 사용할 수 있습니다.|
|변수에 대입할 값|`여친`을 사용할 수 있습니다.|

> 변수명에 $는 사용할 수 없습니다.
> 변수명에 예약어를 사용할 수 있습니다.
> 변수가 이미 선언되었을경우, 변수의 값이 변경됩니다.
> 변수에 대입할 값에 수식을 하나만 넣을 수 있습니다.

코드 예시:  
```
그램$변수명$120

그램$변수명$+변수명

그램$변수명1$변수명

그램$변수명2$여친
```

`그램$변수명$120`은 `변수명` 변수를 선언 한 후, 120으로 초기화합니다.  
`그램$변수명$+변수명`은 `변수명` 변수에 `변수명`의 값을 더합니다.  
`그램$변수명1$변수명`은 `변수명1`에 `변수명`의 값을 대입합니다.  
`그램$변수명2$여친`은 `변수명2` 변수에 여친으로 초기화합니다.  

### 입력

입력은 `재민`으로 처리할 수 있습니다.  

|파라메터|비고|
|------|------|
|대입할 변수 이름|변수가 선언되어있지 않아도 새로 선언합니다.|

> Enter가 입력될 때 변수에 입력 값을 저장합니다.

코드 예시:  
```
그램$변수명$여친

재민$변수명
```

`재민$변수명`은 `변수명`에 입력 값을 저장합니다.

### 출력

출력은 `안산`으로 처리할 수 있습니다. 

|파라메터|비고|
|------|------|
|변수 이름|변수가 선언되어있지 않아도 괜찮습니다.|

> 만약 변수가 선언되어있지 않거나, 변수의 값이 여친이라면, `여친`을 출력합니다.
> stdout으로 출력됩니다.

코드 예시:  
```
그램$변수명$"재민"

안산$변수명
```

`안산$변수명`은 콘솔에 `변수명`을 출력합니다.

### 반복

반복은 `러스트`로 처리할 수 있습니다.

|파라메터|비고|
|------|------|
|값1|값1입니다.|
|값2|값2입니다.|
|줄 번호|이동할 줄 번호입니다.|

> 값1과 값2가 같다면, 줄 번호로 이동합니다.  
> 굳이 이전 줄 번호가 아니여도 됩니다.  

코드 예시:
```
그램$변수명$0

그램$변수명$+1
러스트$변수명$5$1
```

`러스트$변수명$5$1`은 `변수명`의 값이 `5`와 같지 않다면 1번째 줄부터 다시 실행합니다.

## 예제

### 1. Hello World! 출력

```
안산$"Hello World!"
```

### 2. 구구단 출력

```
그램$a$1
그램$b$1
그램$res$1

그램$res$*a

안산$a
안산$" * "
안산$b
안산$" = "
안산$res
안산$"\n"

그램$b$+1
러스트$b$9$4

그램$a$+1
러스트$a$9$4
```

### 3. 입력한 값 출력

```
그램$a$여친
재민$a
안산$a
```
